# Independent review — authorized live draft follow-up

**PASS — no demonstrated acceptance defect found in this follow-up.** This review covers the change from HEAD `d6dc5cca7ddfb79b1890cbf1b7d5bd3e02c06635`, not a new whole-system review.

Reviewer did not implement these changes. Fresh tool smoke confirmed `/root/code/mermail-skills`, the expected HEAD, and ripgrep 15.2.0 (`TEAM_TOOL_SMOKE_OK`). Reviewed the actual source/test diff, observed schemas and reduced write/read receipts before forming this conclusion. No external calls, setup execution, writes to mail, Git mutation, or publication were performed by the reviewer; this receipt is the only reviewer file edit.

All six source/test SHA-256 entries in `review-manifest.json` matched current files. Manifest SHA-256: `d7aebf9f11ddf556f6db92ba733f318f64d917c4d9aa16194ef195ab774143ef`.

| Review boundary | Result and evidence |
| --- | --- |
| Authorized effects | PASS. Inspected `private-evidence/authorized-demo-setup.mjs` without executing it. Its call whitelist permits only list_mailboxes, create_mailbox, save_draft and get_email. Write attempts are persisted before invocation; an already attempted write is refused and there is no automatic write retry. The existing audit records exactly one create_mailbox and one save_draft, with both returned receipts, and no send or wallet invocation. The script is not called by the demo runner. |
| Dedicated self-recipient draft | PASS. `write-audit.json`, `create-mailbox.json`, `mailbox-confirmed.json`, `save-draft.json` and `selected-draft-read.json` agree on the dedicated mailbox and selected draft. The save recipient equals the mailbox's observed address, `send-verifier-demo@mermail.app`. The fixed synthetic body is represented by a SHA-256 in the audit. No existing customer mail was used in these receipts. |
| Live schema and scoped identity | PASS. The mapper consumes observed `id`, scalar `recipient`, `folder_id: draft`, boolean `read`, and optional subject. It maps only the observed draft state; it does not invent sent/processing/delivery/read evidence. The absent email mailbox field is explicitly disclosed: association comes from the matching list_mailboxes identity and successful mailbox-scoped get_email request. The adapter checks selected-record identity before admitting LIVE evidence. |
| Scanner and body boundary | PASS. Fresh selected reads use native objects with metadata_only=true, agent_safe_content=true and require_scan_status=clean, checked against the discovered safe-read schema. The live response's scan_status=null, content_omitted=true and scan_status_not_clean remain limitations. The mapper discards bodies and does not request unsafe content or bypass the scanner. Draft metadata does not imply body inspection or successful transmission. |
| Freshness and origin | PASS. The runner loads only pinned target identifiers, then performs a fresh list_mailboxes and get_email through the collector, with a two-read ceiling. It does not load a saved response as live evidence. Collection/identity errors remain blocked. Synthetic demo content hosted and freshly read is correctly distinguished from fixture evidence. Reviewer replayed the saved read through the mapper/classifier with origin RECORDED and confirmed that origin remained RECORDED. |
| Consumer result | PASS. The replayed actual metadata yields DRAFT_RECORD_FOUND and NOT_SUPPORTED_BY_SELECTED_RECORD. The current report records a fresh LIVE mail capture at `2026-09-18T05:36:30.461Z` with the same narrow assessment; body omission and identity limitations remain visible. `live-demo-final.txt` corroborates the actual fresh runner result. |
| Chain and business separation | PASS. Avalanche source is unchanged in this follow-up. Its current report remains LIVE / RECEIPT_SUCCESS_OBSERVED with public-transaction limitations. Business remains NOT_VERIFIED; the mail draft is not linked to payment settlement, customer ownership or recipient reading. |
| Injection, secrets and output | PASS within scope. New normalization admits bounded metadata only; bodies and unknown fields are not interpreted. Existing report escaping/redaction remains in use. New tests exercise unsafe content, selected read error sanitization, mismatched recipients and missing identities. The scoped 80-file scan reports zero credential findings. The private target is excluded and not exposed by the local server. |
| Regression and delivery checks | PASS. Inspected saved actual output: 123 tests passed, zero failed/skipped, exit 0; validator passed 17 skills/71 business tools. The three changed test files only add tests (no removed expectations); validator and Avalanche source have no diff. HTTP receipts show generated report HTML/JSON 200 and private/repository traversal 403. Diff check passed. |

The reviewer independently checked all manifest hashes and asserted receipt identity/self-recipient consistency, exact audited call sequence/counts, recorded-origin normalization/classification, and current report origins/assessment. Existing complete test, network, HTTP and secret-scan receipts were reused; no live calls or full-suite rerun were necessary for this audit.

Limits: this closes the previous zero-mailbox blocker only for the selected hosted draft. No sent or processing record, delivery, external recipient reading, clean scan/body content, or business/payment relationship is verified. The local audit plus returned/read-back receipts establish the reviewed action sequence; they are not an independent provider-wide account history. No further write, send, commit, push, PR or publication authority is implied by this PASS.
